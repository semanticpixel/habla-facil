# Archived menus

Put retired menu files here if you want to keep them in the repository without
publishing them through GitHub Pages.

GitHub Pages runs Jekyll by default, and Jekyll ignores folders that start with an
underscore. This is still a public repo archive if the repository is public; it is
just not part of the hosted website.
